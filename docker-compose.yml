version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sourceloop
      POSTGRES_PASSWORD: sourceloop
      POSTGRES_DB: sourceloop
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
